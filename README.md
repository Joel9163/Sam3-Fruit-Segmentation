This repository contains a inference pipeline for high-precision fruit segmentationusing the Segment Anything Model 3 (SAM 3).

Unlike standard zero-shot inference, this script implements a two-pass adaptive filtering engine that utilizes reference color matching and local geometric context to eliminate false positives and background noise.
